import React from 'react';
import {
    Image,
    Platform,
    ScrollView,
    StyleSheet,
    Text,
    TouchableOpacity,
    View,
} from 'react-native';

const Header = (props) => (
    <View style={}>
        <Text>{ props.text }</Text>
    </View>
);

export default Header;
